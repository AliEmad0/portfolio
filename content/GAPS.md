# Content gaps to confirm with Ali

The seed in `portfolio.json` uses best-known values. Confirm / fill the following
(these are tracked here rather than as TODO strings inside the JSON, which must always validate):

- [ ] **More projects** beyond PitchIQ — name, one-line summary (en/ar), longer description (en/ar), stack, your role, 2–3 highlights, links (live/repo), screenshot path under `public/projects/`.
- [ ] **Experience timeline** — real entries: start/end (year or `present`), title, org, description (en/ar). Confirm the current single Freelance entry or replace with real history.
- [ ] **LinkedIn URL** — confirm `https://www.linkedin.com/in/ali-emad` is correct (update the `socials` entry if not).
- [ ] **Résumé PDF** — drop the real file at `public/resume.pdf` (referenced by `profile.resumeUrl`).
- [ ] **Project screenshots** — add real images at the `image` paths (e.g. `public/projects/pitchiq.png`).
- [ ] **Skills framing** — confirm categories/items; decide if you want a proficiency indicator (Plan 2 decision).
- [ ] **Arabic prose** — review the seeded Arabic for tone; replace any machine-translated lines you'd word differently.
- [ ] **Contact email** — confirm `sonnythedeveloper@gmail.com` is the address the contact form should reach (Plan 3).
